// Worker uses the same Prisma schema as the API.
// The prisma client is generated from apps/api/prisma/schema.prisma.
// This file re-exports the generated client.
//
// After running `npm run db:generate` in the API package, the @prisma/client
// package in node_modules will have the generated types available for all
// workspace packages.
